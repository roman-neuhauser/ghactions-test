name: ci
on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          echo hello world
          echo "PWD=$PWD"
          echo "########"
          ls -l
